import { SettingsLayout } from "@/features/aluno/settings/SettingsLayout";

export default function ConfiguracoesLayout({ children }: { children: React.ReactNode }) {
  return <SettingsLayout>{children}</SettingsLayout>;
}
