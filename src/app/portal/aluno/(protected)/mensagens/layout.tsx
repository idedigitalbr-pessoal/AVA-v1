import { MessagesLayout } from "@/features/aluno/messages/MessagesLayout";

export default function MensagensLayout({ children }: { children: React.ReactNode }) {
  return <MessagesLayout>{children}</MessagesLayout>;
}
